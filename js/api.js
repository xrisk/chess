//Here is where functions for the api go, it should be self explanitory
function stateOfGame(ID){
	//Do stuff with the api <---???

	//Return -1 for failure
	//Return 0 for create game
	//Return 1 for join game
	//return 2 for game in progress or has ended
	//
	//
	//DIZZY, make sure you follow the comments carefully^^^^
}
function createGame(ID, Color) {

	//Return 1 for success, 0 or fail
}
function joinGame(ID) {
	//Return -1 for fail
	//Return 0 for Black
	//Return 1 for White
}
function submitMove(ID, Move) {
	//Return -1 for fail
	//Return
}
